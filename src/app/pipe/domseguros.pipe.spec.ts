import { DomsegurosPipe } from './domseguros.pipe';

describe('DomsegurosPipe', () => {
  it('create an instance', () => {
    const pipe = new DomsegurosPipe();
    expect(pipe).toBeTruthy();
  });
});
